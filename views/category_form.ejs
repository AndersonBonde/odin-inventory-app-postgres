<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %>></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <%- include('partials/sidebar_nav.ejs') %>

  <main>
    <h1><%= title %></h1>
    <form action="" method="post">
      <div class="form_row">
        <label for="name">Name:</label>
        <input type="text" name="name" id="name" class="form_control" value="<%= undefined === locals.category ? '' : category.name %>" required>
      </div>
      <div class="form_row">
        <label for="description">Description:</label>
        <textarea name="description" id="description" class="form_control" required><%= undefined === locals.category ? '' : category.description %></textarea>
      </div>      
      <button type="submit">Submit</button>
    </form>

    <%- include('partials/errors.ejs'); %>
  </main>
</body>
</html>