<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %>></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <%- include('partials/sidebar_nav.ejs') %>
  
  <main>
    <h1><%= title %></h1>
    <% if (locals.item) { %>
      <h2><%= item.name %></h2>
      <ul style="list-style: none;">
        <li><%= item.description %></li>
        <br>
        <li>Category: <a href="/inventory/category/<%= item.category_id %>"><%= item.category_name %></a></li>
        <li>Price: $<%= item.price %></li>
        <li>In Stock: <%= item.numberinstock %></li>
      </ul>
    <% } %>
  </main>
</body>
</html>